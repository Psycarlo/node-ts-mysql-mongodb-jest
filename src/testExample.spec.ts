// Just a simple test example
describe('Example', () => {
  it('should be an example', () => {
    expect(2 + 2).toEqual(4)
    expect(4).toBeTruthy()
    expect(16).toBeDefined()
    expect(32).toBeGreaterThan(16)
  })
})
